name: confirmatory_pool
seed: 42
images:
  train: 1040
  val: 260
canvas: [720, 960]
objects:
  min: 1
  # Reduzido de 35 para ~18: as 2.093 caixas reais auditadas têm média de
  # 16,3 por imagem (1.628 caixas / 100 fotos de treino). 35 objetos por cena
  # é mais que o dobro da densidade real observada, o que pode empilhar
  # oclusões e artefatos de adaptação de cor atípicos do domínio real.
  max: 18
  min_scale: 0.035
  max_scale: 0.12
  scale_mode: canvas
  rotation_degrees: 180
placement:
  min_depth: 20
  min_visibility: 0.25
  max_attempts_per_object: 100
  z_method: quantile
  z_quantile: 0.65
appearance:
  hardlight_power: 0.65
occlusion:
  edge_blur: 1.5
annotation:
  mode: visible
  min_box_pixels: 2
output:
  jpeg_quality: 95
