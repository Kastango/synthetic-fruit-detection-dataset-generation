name: confirmatory_pool
seed: 42
images:
  train: 1040
  val: 260
canvas: [720, 960]
objects:
  min: 1
  # Densidade original (35), NÃO herda trial001. Isola se o efeito real vem
  # da visibilidade ou da densidade: trial001 (densidade só) piorou o real
  # (n=115); trial002 (densidade+visibilidade) ficou empatado com a
  # baseline. Este candidato testa visibilidade sozinha, sem mexer na
  # densidade, para separar as duas contribuições.
  max: 35
  min_scale: 0.035
  max_scale: 0.12
  scale_mode: canvas
  rotation_degrees: 180
placement:
  min_depth: 20
  min_visibility: 0.4
  max_attempts_per_object: 100
  z_method: quantile
  z_quantile: 0.65
appearance:
  hardlight_power: 0.65
occlusion:
  edge_blur: 1.5
annotation:
  mode: visible
  min_box_pixels: 2
output:
  jpeg_quality: 95
