name: confirmatory_pool
seed: 42
images:
  train: 1040
  val: 260
canvas: [720, 960]
objects:
  min: 1
  max: 35
  min_scale: 0.035
  max_scale: 0.12
  scale_mode: canvas
  rotation_degrees: 180
placement:
  min_depth: 20
  # trial003 testou edge_blur sozinho (piorou); este combina com
  # visibilidade elevada para ver se o efeito muda quando as instâncias
  # muito ocluídas já foram filtradas primeiro.
  min_visibility: 0.4
  max_attempts_per_object: 100
  z_method: quantile
  z_quantile: 0.65
appearance:
  hardlight_power: 0.65
occlusion:
  edge_blur: 3.5
annotation:
  mode: visible
  min_box_pixels: 2
output:
  jpeg_quality: 95
